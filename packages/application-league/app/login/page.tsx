import AuthClientButton from '../components/authButton'

const page = (): React.ReactElement => {
  return (
    <div>
      page login
      <AuthClientButton />
    </div>
  )
}

export default page
